#include <iostream>
#include <Windows.h>
#include "cmdlineSwitchDescriptions.h"

/*
	--stdout, -o		record stdout
	--stderr, -e		record stderr
	--file, -f			file to record output to
	--application, -a	path to application to run/record stdout
	--parameters, -p	parameters to give to the application
	--pid, -i			pid of application to record output
*/
int wmain(int argc, wchar_t* args[])
{

}